from django.urls import path
from .import views
urlpatterns = [
    path("", views.index, name="index"),
    path('index/<uuid:parent_id>/', views.index, name='index_with_parent'),
    path("login/", views.Login, name="login"),
    path("signup/",views.SignUp,name="signup"),
    path("Change_password/",views.Change_password, name="Change_password"),
    path("folder/<uuid:parent_id>/",views.folder, name="folder"),
    path("search_view/<uuid:parent_id>/",views.search_view, name="search_view"),
    path("folder/<uuid:parent_id>/<str:folderNm>/",views.folder, name="folder"),
    path("folder/<uuid:parent_id>/<str:folderNm>/<str:type>/",views.folder, name="folder"),
    path("load_parent/<uuid:parent_id>/",views.load_parent, name="load_parent"),
    path("addfolder/<uuid:parent_id>/", views.addfolder, name="addfolder"),
    path("addfolder_sharing/<uuid:parent_id>/<str:permissions>/<str:parent_name>/", views.addfolder_sharing, name="addfolder_sharing"),
    path("get_file_extension/<str:filename>/", views.get_file_extension, name="get_file_extension"),
    path("logout/", views.Logout, name="logout"),
    path("rename/<uuid:parent_id>/", views.rename, name="rename"), 
    path("back/<str:parent_name>/<uuid:parent_id>/", views.back, name="back"),  # With parent provided
    path("traching_object/", views.traching_object, name="traching_object"),
    path("trash_display_with_move/", views.trash_display_with_move, name="trash_display_with_move"),
    path("trash_display_with_move/<uuid:obj_id>", views.trash_display_with_move, name="trash_display_with_move with obj id"),
    path("restore/<uuid:id>/", views.restore, name="restore"),
    path("delete_trash/", views.delete_trash, name="delete_trash"),
    path("path_change/<uuid:parent_id>", views.path_change, name="path_change"),
    path("path_change_back/<str:parent_name>/", views.path_change_back, name="path_change_back"),
    path("Share_objects/<uuid:parent_id>", views.Share_objects, name="Share_objects"),
    path("display_sharing/",views.display_sharing,name="display_sharing"),
    path("display_sharing/<str:tab>/",views.display_sharing,name="display_sharing_with_tab"),
    path("item_coping/<uuid:parent_id>", views.item_coping, name="item_coping"),
    path("download_folder/<uuid:folder_id>/", views.download_folder, name="download_folder"),
    path("open_file/<uuid:object_id>/", views.open_file, name="open_file"),
    path("inside_sharing_fupload/<uuid:parent_id>/", views.inside_sharing_fupload, name="inside_sharing_fupload"),
    path("inside_sharing_fupload/<uuid:parent_id>/<str:user_permissions>", views.inside_sharing_fupload, name="inside_sharing_fupload"),
    path("inside_sharing_fupload/<uuid:parent_id>/<str:user_permissions>/<str:parent_name>/", views.inside_sharing_fupload, name="inside_sharing_fupload"),
    path("rename_sharing/<uuid:parent_id>/<str:permissions>/<str:parent_name>/", views.rename_sharing, name="rename_sharing"),
    path("load_parent_sharing/<uuid:parent_id>/<str:super_parent>/<str:permissions>/", views.load_parent_sharing, name="load_parent_sharing"),
    path("load_permitted_users/<uuid:object_id>", views.load_permitted_users, name="load_permitted_users"),    
] 
